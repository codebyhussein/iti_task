import 'package:flutter/material.dart';

const Color KmainColor = Colors.blueGrey;

Widget deflutText({required String text}) => Text(
      text,
      style: const TextStyle(fontSize: 30, fontWeight: FontWeight.bold),
    );
